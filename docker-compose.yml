version: '3.8'

services:
  app:
    build:
      context: .
    image: eohsj-audiobook:latest
    restart: unless-stopped
    ports:
      - "3001:80"
    environment:
      - NODE_ENV=production
    labels:
      - "com.docker.compose.project=eohsj-audiobook"
